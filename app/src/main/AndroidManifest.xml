<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roy.clicksend"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <android:uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" /> <!-- <android:uses-permission -->
    <!-- android:name="android.permission.WRITE_EXTERNAL_STORAGE" -->
    <!-- android:maxSdkVersion="18" /> -->
    <!-- <android:uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->
    <!-- <android:uses-permission -->
    <!-- android:name="android.permission.READ_EXTERNAL_STORAGE" -->
    <!-- android:maxSdkVersion="18" /> -->
    <application
        android:name=".MyData"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClickAndSend"
            android:label="@string/title_activity_click_and_send" >
        </activity>
        <activity
            android:name=".EditProfile"
            android:label="@string/title_activity_edit_profile" >
        </activity>
        <activity
            android:name=".HistoryList"
            android:label="@string/title_activity_history_list" >
        </activity>
        <activity
            android:name=".ProfilesPage"
            android:label="@string/title_activity_edit__profile" >
        </activity>
        <activity
            android:name=".ProfilePageRow"
            android:label="@string/title_activity_edit__profile_row" >
        </activity>
        <activity
            android:name=".EditProfileRow"
            android:label="@string/title_activity_edit_profile_row" >
        </activity>
        <activity
            android:name=".click_and_send_row"
            android:label="@string/title_activity_click_and_send_row" >
        </activity>
        <activity
            android:name=".HistoryList_Row"
            android:label="@string/title_activity_history_list__row" >
        </activity>
    </application>

</manifest>
